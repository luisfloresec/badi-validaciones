/**
 * URL base del backend BADI.
 *
 * Para despliegue en producción (Render), cambiar este único valor
 * por la URL pública, por ejemplo:
 *   export const API_BASE_URL = 'https://badi-api.onrender.com';
 *
 * Para desarrollo local, dejar como está:
 */
export const API_BASE_URL = 'http://localhost:3000';
